# Migrations

Add incremental SQL migrations as `*.sql` files in this folder (e.g. `2026-04-27_add_feature.sql`).

They are applied in filename order by `npm run db:migrate` and tracked in the `schema_migrations` table.

